# Система управления личными финансами

Backend-приложение для управления личными финансами на Java.

## Функциональность

- **Авторизация пользователей**: Регистрация и вход по логину и паролю
- **Управление финансами**: Добавление доходов и расходов с категориями
- **Бюджетирование**: Установка бюджета на категории расходов
- **Статистика**: Подсчет и отображение доходов, расходов и состояния бюджета
- **Выгрузка статистики в файл**: Сохранение статистики по доходам/расходам и категориям в текстовый файл
- **Сохранение данных**: Автоматическое сохранение и загрузка данных при запуске/завершении

## Структура проекта

### Исходный код (src/main/)

```
src/main/
├── model/          # Модели данных
│   ├── User.java
│   ├── Wallet.java
│   ├── Transaction.java
│   └── Budget.java
├── service/        # Бизнес-логика
│   ├── AuthService.java
│   ├── WalletService.java
│   ├── BudgetService.java
│   ├── StatisticsService.java
│   └── FileService.java
├── ui/             # Пользовательский интерфейс
│   └── CommandLineInterface.java
└── Main.java       # Точка входа
```

### Скомпилированные файлы (out/)

После компиляции структура сохраняется в папке `out/`:

```
out/
├── Main.class
├── model/          # Скомпилированные модели
│   ├── User.class
│   ├── Wallet.class
│   ├── Transaction.class
│   └── Budget.class
├── service/        # Скомпилированные сервисы
│   ├── AuthService.class
│   ├── WalletService.class
│   ├── BudgetService.class
│   ├── StatisticsService.class
│   └── FileService.class
└── ui/             # Скомпилированный интерфейс
    └── CommandLineInterface.class
```

## Компиляция и запуск

### Использование .bat файлов (Windows)

Самый простой способ компиляции и запуска на Windows:

```bash
# Компиляция проекта
compile.bat

# Запуск приложения
run.bat
```

### Ручная компиляция и запуск

```bash
# Компиляция
javac -d out -sourcepath src/main src/main/*.java src/main/model/*.java src/main/service/*.java src/main/ui/*.java

# Запуск
java -cp out Main
```

**Примечание**: Для корректной работы с русскими символами на Windows рекомендуется использовать `run.bat`, который автоматически настраивает кодировку UTF-8.

## Использование

1. При первом запуске зарегистрируйте нового пользователя
2. Войдите в систему используя логин и пароль (после регистрации вход выполняется автоматически)
3. Добавляйте доходы и расходы, указывая категории
4. Устанавливайте бюджеты на категории расходов
5. Просматривайте статистику в консоли
6. **Выгружайте статистику в файл**: Используйте опцию "Сохранить статистику в файл" для экспорта данных. Файл будет автоматически сохранен с расширением `.txt`, если вы не указали его при вводе имени файла

## Примеры команд

- Добавление дохода: категория "Зарплата", сумма 20000
- Добавление расхода: категория "Еда", сумма 500
- Установка бюджета: категория "Еда", лимит 4000
- Просмотр статистики: автоматический расчет всех показателей

## Формат данных

Данные сохраняются в файлы `логин.txt` для каждого пользователя в следующем формате:
- `USER:логин:пароль` - информация о пользователе
- `TRANSACTION:TYPE:категория:сумма:дата` - транзакция
- `BUDGET:категория:лимит` - бюджет на категорию
